# GENERATED by nav/sim/import_benchmark.py -- do not hand-edit.
#
# Every episode here is one DynaNav COMPLETES on this machine (spl > 0).
# Ordered easiest-first for us, which is not the same as easiest for
# DynaNav: the ranking weights turn size hardest, because that is where our
# runs fail. See the module docstring for the weights and why.
#
# `dynanav_*` fields are the reference numbers to beat, carried alongside so
# a result can be read without opening DynaNav's JSON. Rank candidates by
# spl, never by navigation error: every DynaNav success records ~1.49 m
# because the episode terminates the instant it crosses the 1.5 m threshold.

defaults:
  success_threshold_m: 1.5   # DynaNav's own criterion (benchmark_full.yaml)
  robot_type: "wheeled robot"  # goes into TIC-VLA's system prompt verbatim
  # DynaNav's own Nova Carter cap, and that matters more than it looks: the
  # policy sees its own past motion through previous_waypoints_text, and it
  # plans at the speed it was trained at (measured mean 0.731 m/s asked on
  # office_nearest_elevator). A 0.6 cap clipped nearly every plan, which also
  # threw away the braking the `braking` controller exists to obey -- min(cap,
  # plan) engaged in 11 of 141 calls. Cap above the policy's usual ask and the
  # plan speed is what actually governs.
  max_speed_mps: 1.5
  max_yaw_rate_radps: 1.2
  replan_every_steps: 30     # physics steps between policy calls (60 Hz -> 0.5 s)

episodes:
  office_nearest_elevator:
    # episode_28  |  DynaNav: spl 1.0, path 14.46 m, 21.9 s
    # ours: turn -2.3 deg, straight 15.1 m, detour 0.96x, difficulty 1.19
    scene: "/home/gtu-dsa/robotics/TIC-VLA/DynaNav/assets/office.usd"
    start: [-2.85, 9.6, 0.01]
    start_yaw_deg: -132.0
    goal: [-13.4, -1.2, 0.01]
    instruction: "Go straight ahead and stop at the nearest elevator."
    timeout_s: 70
    dynanav_spl: 1.0
    dynanav_path_m: 14.46
    dynanav_duration_s: 21.9

  hospital_down_hallway:
    # episode_7  |  DynaNav: spl 1.0, path 31.44 m, 46.5 s
    # ours: turn -4.6 deg, straight 32.44 m, detour 0.97x, difficulty 1.51
    scene: "https://omniverse-content-production.s3-us-west-2.amazonaws.com/Assets/Isaac/5.0/Isaac/Environments/Hospital/hospital.usd"
    start: [18.7, 29.3, 0.01]
    start_yaw_deg: -90.0
    goal: [16.1, -3.04, 0.01]
    instruction: "Walk straight down the hallway and stop at the red bed located to the right of the doors."
    timeout_s: 109
    dynanav_spl: 1.0
    dynanav_path_m: 31.44
    dynanav_duration_s: 46.5

  hospital_down_hallway2:
    # episode_10  |  DynaNav: spl 1.0, path 31.23 m, 46.2 s
    # ours: turn -6.4 deg, straight 32.54 m, detour 0.96x, difficulty 1.69
    scene: "https://omniverse-content-production.s3-us-west-2.amazonaws.com/Assets/Isaac/5.0/Isaac/Environments/Hospital/hospital.usd"
    start: [19.7, 29.3, 0.01]
    start_yaw_deg: -90.0
    goal: [16.1, -3.04, 0.01]
    instruction: "Walk straight down the hallway and stop at the red bed located to the right of the doors."
    timeout_s: 109
    dynanav_spl: 1.0
    dynanav_path_m: 31.23
    dynanav_duration_s: 46.2

  office_passing_hallway:
    # episode_49  |  DynaNav: spl 1.0, path 35.21 m, 51.9 s
    # ours: turn -4.3 deg, straight 36.4 m, detour 0.97x, difficulty 1.92
    scene: "/home/gtu-dsa/robotics/TIC-VLA/DynaNav/assets/office.usd"
    start: [-15.2, 12.7, 0.01]
    start_yaw_deg: 90.0
    goal: [-12.5, 49, 0.01]
    instruction: "Go straight ahead, passing through the hallway behind the black glass doors, and then enter the office desk area and stop at the white desk with the plant on it, located on the right side of the wall at the end."
    timeout_s: 140
    dynanav_spl: 1.0
    dynanav_path_m: 35.21
    dynanav_duration_s: 51.9

  hospital_vending_machine:
    # episode_1  |  DynaNav: spl 1.0, path 12.22 m, 18.6 s
    # ours: turn +15.7 deg, straight 13.01 m, detour 0.94x, difficulty 2.14
    scene: "https://omniverse-content-production.s3-us-west-2.amazonaws.com/Assets/Isaac/5.0/Isaac/Environments/Hospital/hospital.usd"
    start: [-3.0, -1.2, 0.01]
    start_yaw_deg: 0.0
    goal: [9.52, 2.33, 0.01]
    instruction: "Go straight ahead and stop at the vending machine at front."
    timeout_s: 54
    dynanav_spl: 1.0
    dynanav_path_m: 12.22
    dynanav_duration_s: 18.6

  hospital_vending_machine2:
    # episode_4  |  DynaNav: spl 1.0, path 11.9 m, 18.2 s
    # ours: turn +15.7 deg, straight 13.01 m, detour 0.91x, difficulty 2.14
    scene: "https://omniverse-content-production.s3-us-west-2.amazonaws.com/Assets/Isaac/5.0/Isaac/Environments/Hospital/hospital.usd"
    start: [-3.0, -1.2, 0.01]
    start_yaw_deg: 0.0
    goal: [9.52, 2.33, 0.01]
    instruction: "Go straight ahead and stop at the front of the vending machine at front left."
    timeout_s: 70
    dynanav_spl: 1.0
    dynanav_path_m: 11.9
    dynanav_duration_s: 18.2

  office_hallway_turn:
    # episode_39  |  DynaNav: spl 1.0, path 13.17 m, 20.0 s
    # ours: turn +12.3 deg, straight 13.74 m, detour 0.96x, difficulty 2.15
    scene: "/home/gtu-dsa/robotics/TIC-VLA/DynaNav/assets/office.usd"
    start: [-10.6, -12.1, 0.01]
    start_yaw_deg: -90.0
    goal: [-7.68, -25.53, 0.01]
    instruction: "Go straight ahead through the hallway, then turn left at the end to approach the red emergency exit door."
    timeout_s: 110
    dynanav_spl: 1.0
    dynanav_path_m: 13.17
    dynanav_duration_s: 20.0

  office_hallway_turn2:
    # episode_37  |  DynaNav: spl 1.0, path 12.71 m, 19.3 s
    # ours: turn +12.9 deg, straight 13.06 m, detour 0.97x, difficulty 2.2
    scene: "/home/gtu-dsa/robotics/TIC-VLA/DynaNav/assets/office.usd"
    start: [-10.6, -12.8, 0.01]
    start_yaw_deg: -90.0
    goal: [-7.68, -25.53, 0.01]
    instruction: "Go straight ahead through the hallway, then turn left at the end to approach the red emergency exit door."
    timeout_s: 90
    dynanav_spl: 1.0
    dynanav_path_m: 12.71
    dynanav_duration_s: 19.3

  hospital_past_wheelchairs:
    # episode_12  |  DynaNav: spl 1.0, path 14.91 m, 29.4 s
    # ours: turn -16.6 deg, straight 15.01 m, detour 0.99x, difficulty 2.28
    scene: "https://omniverse-content-production.s3-us-west-2.amazonaws.com/Assets/Isaac/5.0/Isaac/Environments/Hospital/hospital.usd"
    start: [9.75, 2.82, 0.01]
    start_yaw_deg: -145.0
    goal: [-4.49, -1.91, 0.01]
    instruction: "Go straight past the wheelchairs and stop at the water dispenser by the window."
    timeout_s: 85
    dynanav_spl: 1.0
    dynanav_path_m: 14.91
    dynanav_duration_s: 29.4

  hospital_forward_staircase:
    # episode_17  |  DynaNav: spl 0.879, path 24.4 m, 36.3 s
    # ours: turn +66.1 deg, straight 21.45 m, detour 1.14x, difficulty 7.66
    scene: "https://omniverse-content-production.s3-us-west-2.amazonaws.com/Assets/Isaac/5.0/Isaac/Environments/Hospital/hospital.usd"
    start: [7.38, 1.49, 0.01]
    start_yaw_deg: 90.0
    goal: [-12.23, 10.19, 0.01]
    instruction: "Move forward toward the staircase, then turn left to enter the hallway. Continue straight ahead and stop in front of the blue hospital bed on the right side of the hallway."
    timeout_s: 95
    dynanav_spl: 0.879
    dynanav_path_m: 24.4
    dynanav_duration_s: 36.3

  hospital_exit_room:
    # episode_23  |  DynaNav: spl 0.878, path 28.61 m, 42.5 s
    # ours: turn -82.5 deg, straight 25.12 m, detour 1.14x, difficulty 9.39
    scene: "https://omniverse-content-production.s3-us-west-2.amazonaws.com/Assets/Isaac/5.0/Isaac/Environments/Hospital/hospital.usd"
    start: [23.3, -0.9, 0.01]
    start_yaw_deg: 180.0
    goal: [20, 24, 0.01]
    instruction: "Exit the room and turn right to enter the hallway. Continue straight ahead and to the end of the hallway."
    timeout_s: 145
    dynanav_spl: 0.878
    dynanav_path_m: 28.61
    dynanav_duration_s: 42.5

  # ---- appended verbatim from episodes_manual.yaml ----
  # Not in DynaNav's scored set, so they carry no reference numbers.
  warehouse:
    scene: "https://omniverse-content-production.s3-us-west-2.amazonaws.com/Assets/Isaac/5.0/Isaac/Environments/Simple_Warehouse/full_warehouse.usd"
    start: [-10.22, -7.36, 0.01]
    start_yaw_deg: 90
    goal: [-3.26, 7.61, 0.01]
    instruction: "Proceed to the traffic cones at the entrance of Aisle 05 (the second aisle from the right)."
    timeout_s: 70
    notes: "starts 24.9 deg off the goal bearing, 16.5 m out; NOT VERIFIED by DynaNav"
    # DynaNav's reference run on this machine (benchmark_results/20260813_090804)
    # scored 49 of the 85 episodes -- all hospital and office. It never ran a single
    # warehouse episode, so there is NO published result saying this task is solvable
    # by TIC-VLA. The only warehouse number that exists anywhere here is the SMOKE
    # episode, which DynaNav itself failed at 31.02 m. Do not treat a failure on this
    # episode as evidence of a bug in this repo until a verified episode passes.
  # DynaNav episode_76. The best-aimed warehouse episode in the whole benchmark, and a
  # much longer run -- useful for checking the robot still tracks an instruction after
  # it has had to commit to a turn.
  warehouse_aisle6:
    scene: "https://omniverse-content-production.s3-us-west-2.amazonaws.com/Assets/Isaac/5.0/Isaac/Environments/Simple_Warehouse/full_warehouse.usd"
    start: [-13.22, -6.36, 0.01]
    start_yaw_deg: 59
    goal: [1.82, 24.5, 0.01]
    instruction: "Go straight to enter Aisle 06 (the last aisle from the left), passing the yellow forklift to reach the end of the aisle."
    timeout_s: 250
    notes: "starts 5.0 deg off the goal bearing, 34.3 m out; NOT VERIFIED by DynaNav"
  # DynaNav episode_6 -- and DynaNav FAILED it: navigation_error 17.86 m, spl 0.00.
  # Kept because a known-failing episode is a useful negative control, but do not use
  # it to judge this repo. Note how little separates it from episode_7 below, which
  # DynaNav passes with spl 1.00: start x 19.7 vs 18.7, timeout 55 s vs 65 s.
  # ---------------------------------------------------------------------------------
  # OUTDOOR -- the fourth DynaNav scene, and the only one this repo has never run.
  #
  # `benchmark_full.yaml` defines 85 episodes over four scenes: hospital 25, office 25,
  # warehouse 25, outdoor_small 10. DynaNav's own reference run covers 49 of them, all
  # hospital and office, so `import_benchmark.py` -- which ranks by DynaNav's measured
  # result -- can never emit an outdoor episode. Same reason the warehouse pair above is
  # hand-written. There is NO reference number for any of these; a failure here is not a
  # regression against anything, and a success is not a win over anything either.
  #
  # Six of DynaNav's ten, chosen for two reasons. Four of the ten are duplicate tasks
  # differing only in `timeout` and crowd size (51/52, 53/54, 55/56, 59/60), so the more
  # generous twin is kept and the other dropped rather than scoring the same task twice.
  # And episode_60 starts 57.7 deg off its own goal bearing, outside the +-30 deg rule
  # this config has followed since three of the four smoke episodes turned out to spawn
  # the robot facing away from its goal -- which looks exactly like a broken policy.
  # Off-bearings below are computed, not eyeballed, and all six are inside 18 deg.
  #
  # TWO THINGS THAT MAKE THESE NOT A LIKE-FOR-LIKE COMPARISON WITH DYNANAV, both of which
  # must be stated with any number that comes out of them:
  #
  #   CROWDS. Every one of these episodes carries `num_people: 100..200`, and DynaNav
  #   spawns those as animated pedestrians. `build_nav_scene.sh` authors a static stage,
  #   so our street is EMPTY. That makes our version of the task easier, not harder, and
  #   in the one way that matters most for a benchmark whose name is DynaNav -- the
  #   dynamic obstacles are the point of it.
  #
  #   ELEVATION. This is the first scene with real terrain: starts sit at z 6.25-8.5 and
  #   goals at z 5.97-6.55, and episode_59's instruction is literally "go down the ramp".
  #   The base is a KINEMATIC teleport that integrates x/y and re-syncs only past 0.25 m,
  #   and `collision_guard.py` casts a horizontal ray fan. Neither has ever been asked to
  #   handle a slope. Expect this to be where they break, and check the trace's z before
  #   reading any outdoor failure as a navigation failure.
  outdoor_umbrellas:
    scene: "${TICVLA_DYNANAV_ROOT}/assets/outdoor_small.usd"
    start: [26.9, 148.8, 6.85]
    start_yaw_deg: 160
    goal: [9.39, 154.52, 5.97]
    instruction: "Go straight until reach the black and white striped umbrellas."
    timeout_s: 150
    success_threshold_m: 4.0
    notes: "DynaNav episode_52; 1.9 deg off bearing, 18.4 m; 200 people in theirs, none in ours"
  outdoor_pillars:
    scene: "${TICVLA_DYNANAV_ROOT}/assets/outdoor_small.usd"
    start: [-24.79, 173.1, 8.5]
    start_yaw_deg: 32
    goal: [2.2, 193.8, 6.55]
    instruction: "Drive forward passing the pillars and stop when you reach the last pillar at the far end."
    timeout_s: 200
    success_threshold_m: 4.0
    notes: "DynaNav episode_54; 5.5 deg off bearing, 34.0 m; descends 1.95 m"
  # The one with an explicit negative in the instruction ("Do not turn right"). Worth
  # having: the arc menu obeys a direction word hard -- measured at 103 deg of authority
  # -- and nothing here has ever tested whether it obeys a PROHIBITION the same way.
  outdoor_library:
    scene: "${TICVLA_DYNANAV_ROOT}/assets/outdoor_small.usd"
    start: [-34.0, 150.6, 6.25]
    start_yaw_deg: 80
    goal: [-24.02, 173.3, 6.3]
    instruction: "Cross the street straight ahead, then proceed forward to the Northside Branch Library with the circular window at front. Do not turn right."
    timeout_s: 250
    success_threshold_m: 4.0
    notes: "DynaNav episode_56; 13.7 deg off bearing, 24.8 m; tests a prohibition, not a direction"
  # A 1 m threshold over 9.8 m. The tightest stop anywhere in this config -- the whole
  # task is arriving precisely, which is exactly what the model-chosen approach speed
  # was added for.
  outdoor_upsway:
    scene: "${TICVLA_DYNANAV_ROOT}/assets/outdoor_small.usd"
    start: [2.26, 147.4, 8.0]
    start_yaw_deg: -90
    goal: [2.26, 137.6, 6.09]
    instruction: "Cross the street and continue straight until you reach the storefront with the yellow UPSWAY sign."
    timeout_s: 250
    success_threshold_m: 1.0
    notes: "DynaNav episode_57; 0.0 deg off bearing, 9.8 m; 1 m threshold, descends 1.91 m"
  # Same destination as the one above from four times the distance and a 2 m threshold.
  # The pair isolates approach precision from route finding: same stopping problem, very
  # different amount of driving to get to it.
  outdoor_upsway_far:
    scene: "${TICVLA_DYNANAV_ROOT}/assets/outdoor_small.usd"
    start: [-11.5, 173.4, 8.0]
    start_yaw_deg: -54
    goal: [2.26, 137.6, 6.09]
    instruction: "Cross the street and continue straight until you reach the storefront with the yellow UPSWAY sign."
    timeout_s: 300
    success_threshold_m: 2.0
    notes: "DynaNav episode_58; 15.0 deg off bearing, 38.4 m; same goal as outdoor_upsway"
  outdoor_ramp_fountain:
    scene: "${TICVLA_DYNANAV_ROOT}/assets/outdoor_small.usd"
    start: [-18.04, 111.52, 6.5]
    start_yaw_deg: 60
    goal: [-10.5, 146.22, 5.99]
    instruction: "Continue forward, go down the ramp by the slope on the left side of the red triangular yield sign, then head toward the fountain."
    timeout_s: 200
    success_threshold_m: 4.0
    notes: "DynaNav episode_59; 17.7 deg off bearing, 35.5 m; the explicit ramp episode -- the kinematic base's hardest case. DynaNav's own text reads 'slopeon'; the space is added here because a frozen Qwen reads this prose literally and a run-on word is a distractor, not part of the task. This is the only wording changed from the benchmark anywhere in this config."
